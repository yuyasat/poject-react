export interface TopState {
  firstColumn: number;
  secondColumn: number;
  firstRow: number;
  secondRow: number;
  firstColor: number;
  secondColor: number;
}

export interface GridState {
  color: number;
}